version: '1.0'

steps:

  Helm Upgrade:
    title: Helm Upgrade
    image: 'codefresh/plugin-helm:2.8.2'
    environment:
      - CHART_NAME=$
      - RELEASE_NAME=$
      - KUBE_CONTEXT=$
      - NAMESPACE=$
      - DEBUG_CHART=$
      - CHART_REPO_URL=$
      - CUSTOMFILE_test=$
    